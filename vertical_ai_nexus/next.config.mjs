/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'hebbkx1anhila5yf.public.blob.vercel-storage.com', // Allow the external domain for images
      ],
    },
  };
  
  export default nextConfig;
  